In this directory open a cmd prompt

java -cp bin cisc187.bigcalc.Calc < input.txt 

will print to cmd prompt

java -cp bin cisc187.bigcalc.Calc < input.txt > output.txt

will print to output.txt in this directory

java -cp bin cisc187.bigcalc.Calc

will run the program natively and you can enter in expressions via command prompt
type in EXIT to close
